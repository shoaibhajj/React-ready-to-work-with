// lib imports

import { Outlet } from "react-router-dom";

// components imports

// assets imports

const MainLayout = () => {
  return (
    <main>
      <Outlet />
    </main>
  );
};

export default MainLayout;
